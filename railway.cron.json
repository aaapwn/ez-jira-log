{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "buildCommand": "docker build -f apps/server/Dockerfile.cron -t ez-jira-log-cron .",
    "dockerfilePath": "apps/server/Dockerfile.cron"
  }
}
